# React Native / Expo (Metro) cannot resolve pnpm's symlinked node_modules,
# so the workspace uses a hoisted (flat) node_modules layout.
# See: https://docs.expo.dev/guides/monorepos/
node-linker=hoisted
